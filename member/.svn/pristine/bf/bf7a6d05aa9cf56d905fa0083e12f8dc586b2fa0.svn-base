$(function (){
	$('#tt').tabs('onSelect') 
	var tab = $('#tt').tabs('getSelected');
	var index = $('#tt').tabs('getTabIndex',tab);
	alert(index);
	
});
/*$(function() {
	var tabs = $("#tt").tabs().tabs('tabs');
	console.debug(tabs);
	//循环出每一个tab面板,鼠标选中事件
	for ( var index = 0; index < tabs.length; index++) {
		var currentTab = tabs[index];
		var tab = currentTab.panel("options").tab;
		tab.mouseenter({
			idx : index
		}, function(event) {
			$("#tt").tabs("select", event.data.idx);
		});
	}
	;
	console.debug(index);//js中的循环不同,index在for循环里,但它是全局的
});*/